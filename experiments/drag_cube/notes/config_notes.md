
## Notes migrated from config_latent_cube.json

- `optimization.pca._note`: 

## Notes migrated from config_latent_cube_dafoam.json

- `optimization.pca._note`: off: the latent codes are the design variables directly

## Notes migrated from config_latent_cube_with_cylinders.json

- `optimization.pca._note`: 

## Notes migrated from config_probe_dafoam_start.json

- `optimization.pca._note`: off: the latent codes are the design variables directly

## Notes migrated from config_probe_dafoam_opt.json

- `optimization.pca._note`: off: the latent codes are the design variables directly

## Notes migrated from config_probe_openfoam_start.json

- `optimization.pca._note`: off: the latent codes are the design variables directly

## Notes migrated from config_probe_openfoam_opt.json

- `optimization.pca._note`: off: the latent codes are the design variables directly

## Mesh blocks

The cylinders, holes and FFD configs were snappyHexMesh runs; their `mesh.sdf_hex` block was copied from `config_latent_cube.json` at the migration (2026-09-23) and has not been tuned for these geometries.
